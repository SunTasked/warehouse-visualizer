# dependencies
node_modules/

# build output
dist/
dist-ssr/
build/
out/
.turbo/
.next/
.vite/
storybook-static/
*.tsbuildinfo

# env
.env
.env.*.local
.env.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# test / coverage
coverage/
playwright-report/
test-results/

# source plant data — real operational records (picking history with operator
# initials), kept local and never published. The layouts imported *from* it
# under schema/ carry only location codes and geometry. Root-anchored: a bare
# `data/` also matched src/data/ and silently kept new source files out.
/data/
__pycache__/

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
